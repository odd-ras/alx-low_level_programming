#include "main"

/**
 * _isupper - check for upper case letters
 * @c: character to be checked
 * Return: void
 */

int _isupper(int c)
{
	if (isupper(c))
	{
		return (1);
	}
	else
	{
		return (0);
	}

}
