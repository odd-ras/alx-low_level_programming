#include "main.h"

/**
 * add - main entry
 * @int: integer values
 * Description: adds two integers and
 * returns the result
 *
 * Return: sum
 */
int add(int, int)
{
	int x, y;
	int sum = 0;

	sum = x + y;

	return (sum);
}
