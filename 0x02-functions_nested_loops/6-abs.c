#include "main.h"
#include <stdlib.h>

/**
 * _abs - main entry
 * @int: an integer
 * Description: computes absolute value of
 * an integer
 * Return: 0
 */
int _abs(int)
{
	int x = abs(int);

	return (x);
}
