#include "main.h"
#include <stdlib.h>

/**
 * _abs - main entry
 * @x: an integer
 * Description: computes absolute value of
 * an integer
 * Return: 0
 */
int _abs(int x)
{
	int x = abs(int);

	return (x);
}
